# 任务1交付包：框架放置与融合方案

> 分析对象：`任务1交付包/`（监管问询语料解析与风险关注点抽取，官方指标达标：分类准确率 92.6% ≥ 80%）
> 结论：任务1 交付包不是"一个 Agent"，而是**全系统的"标签基础设施"**——落点应分散到
> `backend/data/labels/`（运行时加载）+ `backend/skills/rule_risk_extract.py`（新 Skill）+ `backend/scripts/`（离线管线）。

---

## 一、交付包盘点（10 个文件，5 个目录）

| 目录 | 内容 | 性质 |
|------|------|------|
| 01_标注规范 | 标注规范 v1.1 冻结版（A-H 8 一级 / 45 二级 / Q01-13 / P01-08）、试标说明 | 标签体系标准 |
| 02_分类结果 | **全量分类结果_10481条.jsonl**（★核心成品：10,481 条关注点带 L1/L2/质疑性质/环节/置信度/原因） | 标签库成品 |
| 03_配置文件 | **risk_dictionary.yaml**（风险词典）、risk_taxonomy.yaml、risk_label_mapping.csv、新旧标签映射表、分类提示词_v2.txt | 复现/运行时加载 |
| 04_评估报告 | 任务1交付报告.md（方法/指标/局限） | 交付文档 |
| 05_脚本 | run_llm_classify.py / eval_llm_classify.py / sample_for_annotation.py / analyze_focus_points.py | 离线分类管线 |

---

## 二、应放在框架的哪个位置（目标布局）

```
D:\competition_agent\
├── backend\
│   ├── data\
│   │   └── labels\                     ★ 任务1产物核心落点（运行时被 Agent/Skill 加载）
│   │       ├── risk_taxonomy.yaml          标签体系（A-H/45二级/Q/P，全系统标签标准）
│   │       ├── risk_dictionary.yaml        ★ 风险词典（规则抽取用，之前缺失的关键资产）
│   │       ├── risk_label_mapping.csv      新旧标签映射
│   │       ├── 新旧标签映射表.csv
│   │       ├── classification_prompt_v2.txt  分类提示词（对齐官方口径）
│   │       └── classified_focus_points_10481.jsonl  全量分类结果（标签/证据库成品）
│   ├── skills\
│   │   └── rule_risk_extract.py        ★ 新 Skill：读 risk_dictionary.yaml 做规则风险抽取
│   ├── scripts\
│   │   ├── run_llm_classify.py         （队友脚本迁入：可重跑/复现分类管线）
│   │   ├── eval_llm_classify.py
│   │   ├── sample_for_annotation.py
│   │   └── analyze_focus_points.py
│   └── data\output\
│       └── 任务1交付报告.md  +  标注规范    （交付文档归档）
```

---

## 三、它的意义（对系统、对赛题）

### 1. 赛题硬指标的达标凭证（必须交付）
任务1 = 赛题四大攻关任务之一：**监管问询语料解析与风险关注点抽取**。
本包直接满足官方指标"监管关注点分类准确率 ≥ 80%"，实测 **92.6%**（一级 Macro-F1 86.8%、二级准确率 81.2%、300 条双人标注 Kappa 0.889）。交付报告可直接提交。

### 2. ★ 标签体系 = 全系统的"统一语言"（最大价值）
我们之前一直在做的"label_ref / category 对齐"（case_retriever 的 LABEL_KEYWORDS、attributor 的 label_ref、公告研读的 category）——**这套 A-H/45 二级标签体系就是标准答案**：
- 公告研读的 category 枚举 → 对齐 A-H 体系
- 案例检索的标签通道 → 用官方 L2 标签（替代/扩充硬编码 LABEL_KEYWORDS）
- 归因的 label_ref → 对齐官方主题
→ 全系统标签口径统一 = **与官方评测一致 = 提高"案例匹配 Top-5 命中率 / 逻辑解释有效性"评分**。

### 3. ★ risk_dictionary.yaml = 规则风险抽取器的地基（落地 P1 融合点）
之前《监管问询解析_融合分析》里说"规则引擎需要队友共享 risk_dictionary.yaml"——**就在这个包里**！
可直接写 `backend/skills/rule_risk_extract.py`（否定词 + 证据上下文规则），
公告研读升级为**三通道抽取**：规则（零成本兜底）→ FinBERT 门控 → LLM 精细，交叉校验防漏防幻觉。

### 4. 全量分类结果 = 现成标签/证据库（下游直接用）
10,481 条已分类监管关注点（含 L1/L2/质疑性质/环节/置信度/判定原因）：
- **丰富案例库**：case_db 的 focus_points 补上官方 L2 标签 → 案例检索标签通道更强；
- **证据来源**：归因 Agent 可直接引用"这条关注点官方判定为 C06"作为佐证；
- **置信度筛选用**：confidence < 0.7（约 2.3%）标记人工复核（队友 README 建议）。

### 5. 分类提示词 = 对齐官方口径的 Prompt 模板
我们的 LLM 抽取 Prompt（公告研读/案例解析）的类别枚举应对齐 A-H 体系与质疑性质 Q01-13，
让 LLM 输出天然贴近官方表述 → 评测（ROUGE/语义相似度）得分更高。

---

## 四、任务1在框架中的角色定位（不是独立 Agent）

| 竞赛任务 | 在框架中的实现 |
|---------|--------------|
| 任务1：语料解析+关注点抽取 | ① 标签体系（labels/）② 公告研读 Agent（对公告抽取）③ 案例库构建（对问询函解析）④ 规则抽取 Skill（新增） |
| 任务2：概率预测 | 预测建模 Agent（F1-F6，含队友 365 天监管语义特征做 F6） |
| 任务3：Agent 编排 | SweepingOrchestrator（已实现） |
| 任务4：可解释报告 | 案例检索（RRF）+ 归因 + 报告 Agent（已实现 2/3） |

---

## 五、落地清单

- [ ] 建 `backend/data/labels/`，复制 03_配置文件 + 02_分类结果
- [ ] 写 `backend/skills/rule_risk_extract.py`（读 risk_dictionary.yaml）
- [ ] case_retriever 的 LABEL_KEYWORDS 改为加载 risk_taxonomy/分类结果（或对齐 L2 标签）
- [ ] 公告研读/案例解析的 Prompt category 枚举对齐 A-H 体系
- [ ] 案例库重建时 focus_points 补官方 L2 标签
- [ ] 队友脚本迁入 backend/scripts/（可选，用于重跑/复现）
- [ ] 交付报告归档 backend/data/output/
