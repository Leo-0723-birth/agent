# 监管问询扫雷预警系统 · 优化审查报告

## 一、F1-Score 问题已修复

### 问题现象
- 主控 Dashboard 与预测建模 Agent 详情页中的 F1-Score 长期显示为 **0.71**，与真实模型评估结果（约 0.34）不符。
- 切换为离线数据后，指标仍未变化，给用户"没有反馈"的错觉。

### 根因
- 前端 `api/static/index.html` 中的指标卡片是**写死常量**：AUC 0.8335、覆盖率 46.1%、F1 0.71。
- 后端 `/api/company/{code}` 返回的数据中缺少 `windowPredictions[].metrics`，导致前端无法从真实模型评估结果取值。
- 因此无论选择哪家公司、是否离线/实时，数字都不会变化。

### 修复内容
1. **后端**：`api/pipeline.py` 新增读取 `backend/models/predictor/model_summary.json`，将每个窗口的 Ensemble 真实指标（AUC、F1、Top10%Recall、threshold）注入 `WindowPrediction.metrics`。
2. **前端**：
   - Dashboard 顶部三指标改为通过 `renderModelMetrics()` 从 60 天窗口的 `metrics` 动态渲染。
   - 预测建模 Agent 详情页"模型性能指标"区域改为按当前选中窗口（30/60/90 天）动态读取 `selected.metrics`。
   - 修复 `Top10%Recall` 因含 `%` 字符导致 JS 点号访问报错的语法问题，改用 `m['Top10%Recall']`。
3. **验证**：五粮液 60 天窗口现在显示 **F1=0.3386、AUC=0.8335、Top10%Recall=46.1%**，与 `model_summary.json` 完全一致。

---

## 二、全面优化审查

以下从 **表达清晰度、逻辑结构、信息完整性、沟通效果** 四个维度，对当前 Demo 进行审查并给出可执行建议。

### 2.1 表达清晰度

| 问题 | 影响 | 优化建议 | 优先级 |
|---|---|---|---|
| 离线/实时状态标签不统一 | 用户难以判断当前数据是快照还是实时结果 | 统一左上角状态文案，例如"📷 离线快照 · 2026-08-25" / "⚡ 实时扫雷 · 刚刚完成"；主控公司名称旁也同步显示 | 高 |
| "可解释追踪率 100%" 含义模糊 | 评委可能质疑这个数字是如何计算出来的 | 改为"Agent 链路可追溯"并增加悬停提示说明：7 个 Agent 均输出 trace_log / evidence_anchor | 中 |
| 风险等级阈值文案只在中控出现 | 预测建模详情页、风险因子卡片缺少阈值说明 | 在风险概率仪表盘下方固定展示"低 < 30% / 中 30%-70% / 高 ≥ 70%" | 中 |
| "置信度" 与 "AUC" 概念混用 | 普通用户会混淆"模型置信度"和"模型区分度" | Dashboard 的"预测置信度"改为"预测置信度（模型输出概率）"，AUC 统一叫"模型区分度 AUC" | 中 |

### 2.2 逻辑结构

| 问题 | 影响 | 优化建议 | 优先级 |
|---|---|---|---|
| 左侧 7 个 Agent 卡片与顶部 5 步流水线映射不一致 | 用户不知道"段落召回"为什么在顶部没有对应节点 | 在顶部流水线 tooltip 中说明：5 步为聚合展示，左侧 7 张卡片为完整执行链路；或把"段落召回"合并到"归因分析"步骤中 | 高 |
| 主控 Tab "比赛历史库" 名称与赛题场景略脱节 | 听起来像赛事页面，而非监管问询历史 | 建议改为"历史问询库"或"同案例库"，与功能更匹配 | 中 |
| 预测建模详情页的"模型指标与解释"表格与上方"模型性能指标"卡片重复 | 信息冗余，评委需要滚动才能看到 SHAP | 合并为"模型性能"+"SHAP 解释"两大区块，删除重复表格或只保留关键阈值说明 | 中 |
| Agent 详情页返回主控的入口只有一个左上小链接 | 演示时容易迷失 | 在每个 Agent 详情页底部增加醒目的"返回主控"按钮，与顶部链接形成双向导航 | 中 |
| 财务异常详情页目前只展示表格 | 缺少异常指标的视觉层级（高/中/低） | 增加 severity 徽章列和按严重程度排序的默认排序 | 中 |

### 2.3 信息完整性

| 问题 | 影响 | 优化建议 | 优先级 |
|---|---|---|---|
| 案例匹配 Agent 只展示余弦相似度，缺少 RRF 分数 | 与原 Streamlit 方案能力不一致，显得降配 | 后端 `similarCases` 已支持扩展字段，可在前端增加 RRF 分数列（后端补充 `rrfScore` 字段） | 高 |
| 公告研读详情页缺少"公告清单"Tab 的原始公告链接 | 证据链不完整 | 为每条风险证据增加"查看原文"按钮，跳转到 `sourceUrl`（后端已有该字段） | 高 |
| 归因分析详情页缺少"因子的反事实解释" | 只列证据，没有说明"为什么这个因子拉高/拉低风险" | 对每个因子增加一句话解释，如"该指标高于行业均值 1.8 倍，历史同情况公司被问询概率提升 3.2 倍" | 中 |
| 报告生成详情页缺少导出 Word/PDF 入口 | 比赛展示时无法直接导出交付物 | 增加"下载 Word 报告"按钮，调用 `/api/report/{code}` 或前端生成 Markdown 转 docx | 中 |
| 实时扫雷完成后没有"任务耗时"展示 | 无法体现流水线效率 | 在实时结果页顶部增加"本次扫雷耗时 xx 秒"和"Agent 步数 7/7" | 低 |

### 2.4 沟通效果

| 问题 | 影响 | 优化建议 | 优先级 |
|---|---|---|---|
| 首页引导文案过短，没有说明系统能力 | 首次使用的评委不知道 Demo 核心亮点 | 首页增加 3 个价值点卡片：① 多 Agent 协同 ② 可解释预测 ③ 30/60/90 天多窗口 | 中 |
| 实时扫雷失败时的 fallback 提示过于技术性 | 普通用户看到"WebSocket 连接断开"会困惑 | fallback 提示改为"实时扫雷已切换为离线快照，仍可继续展示"，并在折叠区域展示原始错误 | 中 |
| 风险证据列表缺少"为什么这条证据重要"的总结 | 评委需要自己归纳 | 每个 Tab 顶部增加一句话"当前公司 xx 条风险证据中，文本类 x 条、财务类 x 条、市场类 x 条" | 中 |
| 截图/录屏演示时缺少"当前步骤"高亮 | 静态页面不易体现交互流程 | 在左侧导航增加当前选中步骤的高亮态，并确保滚动时自动高亮对应 section | 低 |

---

## 三、可快速落地的 5 项优化

如果时间和优先级有限，建议先做以下 5 项，性价比最高：

1. **统一离线/实时状态标签**（高）：让用户一眼识别数据来源。
2. **修复案例匹配 RRF 分数展示**（高）：补齐与原方案的对齐度。
3. **增加公告风险证据"查看原文"链接**（高）：补全可追溯性。
4. **合并预测建模详情页重复指标表格**（中）：减少信息冗余。
5. **首页增加 3 个价值点卡片**（中）：提升 Demo 开场说服力。

---

## 四、已验证的当前状态

- F1-Score：0.3386（五粮液 60 天窗口）
- AUC：0.8335
- Top10% 覆盖率：46.1%
- 分类阈值：0.3022
- 预测建模详情页支持 30/60/90 天窗口切换，指标随窗口联动。
- Dashboard 顶部指标在切换公司或实时扫描完成后会自动刷新。

---

*报告生成时间：2026-08-27*
