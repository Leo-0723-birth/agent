# 监管问询解析（队友 02_监管问询）与公告研读 Agent 融合分析

> 分析对象：
> - 我方：`D:\competition_agent\backend\agents\announcement_reader.py`（公告研读 Agent，在线）
> - 队友：`D:\新建文件夹\02_监管问询\`（监管问询解析管线，离线批量）
> 结论：**两者互补，可融合"产物"（向量库/特征表/规则引擎），不融合"运行模式"**。

---

## 一、队友的 02_监管问询 是什么（盘点）

| 子目录 | 产物 | 说明 |
|--------|------|------|
| 01_数据清单与结构化文本 | `all_inquiries_10056.jsonl`、`inquiries.jsonl` | **10,056 份问询函+回复**的结构化文本（doc_id/标题/发函日/段落级文本/表格/解析状态） |
| 02_风险标签 | `inquiry_rule_risks.jsonl` | **规则引擎**抽取的风险标签（否定词窗口 16 字符 + 证据上下文 80 字符，确定性、可解释） |
| 03_向量索引与Chroma | `inquiry_embedding_index.jsonl` + `regulatory_inquiry_chunks/`（ChromaDB）+ `inquiry_embeddings.npy` | **BGE-large-zh-v1.5（1024 维，CUDA）**分块向量库（chunk 420 字/重叠 80 字） |
| 04_365天独立语义特征 | `regulatory_inquiry_letter_semantic_features.parquet`（37,222 行 × 300 维）、`*_pca.npz` | **365 天窗口 + 时间衰减池化（half_life 90 天）→ PCA 300 维**；只用 inquiry_letter，不混 reply |
| 05_标签评测与报告 | `evaluation_ground_truth_normalized.csv`、`official_company_label_alignment.csv`、`future_inquiry_targets.parquet` | 官方标签对齐 + 30/60/90 天未来问询标签 |
| 06_代码配置 | `build_source_separated_features.py`、`regulatory_announcement_full.yaml`、`source_feature_split.py` | 管线入口与配置（announcement_nlp 包，机器在 `D:/比赛数据/`） |

---

## 二、差异点（两者本质不同）

| 维度 | 公告研读 Agent（我方） | 02_监管问询（队友） |
|------|----------------------|---------------------|
| **分析对象** | **目标公司的公告**（公司自己披露了什么） | **历史监管问询函+回复**（监管问了什么） |
| **语义视角** | "公司现状信号"（我有什么风险） | "历史被问询知识/先例"（什么样会被问） |
| **建模用途** | F1 语义特征 + 风险要素（喂预测/案例检索/归因） | F6 历史监管语义特征 + 案例检索库（喂模型训练） |
| **运行模式** | 在线、单公司、每次扫雷实时跑 | 离线、全量批处理、一次性建好 |
| **抽取方式** | FinBERT 门控 + LLM 精细抽取（evidence 强制原文） | 规则引擎（否定词 + 证据上下文，确定性） |
| **向量化** | embedding（bge/fallback）→ 内存/npy 小库 | BGE 1024 维 → ChromaDB + npy + PCA 300 维 |
| **时间处理** | 近一年窗口，无衰减 | 365 天窗口 + **时间衰减池化**（half_life 90 天） |
| **产出形态** | ctx.semantic（结构化，进 Agent 流水线） | parquet / ChromaDB / npy（特征表，进模型训练） |
| **评测红线** | — | README 自注：evaluation_ground_truth 只用于评测，**不得作为预测特征** |

**一句话**：队友做的是"监管视角的**训练特征管线**"，我们做的是"公司视角的**在线分析 Agent**"。互补关系——一个回答"历史上什么情况被问过"，一个回答"这家公司现在有什么信号"。

---

## 三、可融合进公告研读 Agent 的点（按优先级）

### P0-1：案例检索向量源升级 → 队友的 BGE 向量库（收益最大）
- 队友已建好：`inquiry_embeddings.npy`（BGE 1024 维）+ `inquiry_embedding_index.jsonl`（chunk 元数据）+ ChromaDB
- 融合：`backend/skills/vector_store.py` 加 **ChromaDB 后端**（或直接读 npy+index 构建向量矩阵），
  案例检索 Agent 的语义通道从"fallback 65536 维小库"升级为"BGE 1024 维全量库"
- 注意：队友库是 **chunk 级**向量（不是整封函），检索后需按 `announcement_id` **聚合 chunk 得分**再排序
- 效果：检索质量显著提升（fallback 字符 bigram → BGE 语义向量）

### P0-2：365 天监管语义特征 → F6 历史监管特征（直接可用）
- `regulatory_inquiry_letter_semantic_features.parquet`（37,222 行 × 300 维，按公司×时间窗口）
- 融合：预测模型训练时读 parquet 作为 **F6 特征族**（需装 pyarrow）；
  在线推理按"公司 + 窗口截止日 T"查表（窗口在 T 之前 → 无时间泄漏）
- 与公告研读 F1（公司视角）互补成完整特征拼图

### P1：规则风险抽取器 → `skills/rule_risk_extract.py`
- 队友的规则引擎（否定词处理 + 证据上下文）是"零成本、确定性、可解释"的抽取
- 融合：公告研读加**第三条抽取通道**：规则抽取（兜底/交叉验证）→ FinBERT 门控 → LLM 精细，
  规则与 LLM 结果交叉校验（防漏报 + 防幻觉）
- ⚠️ 依赖队友的 `risk_dictionary.yaml`（风险词典），需其共享；否则先写简单关键词规则版

### P2：理念借鉴（不强依赖）
1. **时间衰减池化**：队友 365 天 + half_life 90 天衰减 → 我方 F1 可借鉴（当前一年平铺）；
2. **Chunking 参数**（420/80/20）与"letter 不混 reply"经验 → 我方案例库构建复用；
3. **Embedding 模型缓存**：队友本地已有 bge-large-zh-v1.5（models/embedding/），我方 embedding.py 的 bge 后端可指向同一缓存避免重复下载。

---

## 四、不能直接融合的（差异红线）

1. **运行模式不同**：队友是离线批量特征管线（训练用），我方是在线单公司 Agent（推理用）——
   不合并代码，**只消费对方的产物**（npy/parquet/jsonl/Chroma），避免耦合队友的 announcement_nlp 包；
2. **评测红线**：`evaluation_ground_truth` 只用于归因/语义抽取评测，**绝不能作为预测模型输入特征**
   （队友 README 已强调）。我方案例库用官方 GT 标签是"归因/报告层"用途，合法；但预测特征必须与 GT 隔离；
3. **规则词典依赖**：规则引擎需要队友的 risk_dictionary.yaml，未共享前无法复跑其规则抽取。

---

## 五、融合后的特征拼图

```
F1 公告语义（在线，我方）   ← 公告研读 Agent（风险要素 + F1 向量）
F6 监管语义（离线，队友）   ← 365天监管语义特征 parquet（300 维，BGE）
案例检索向量（队友资产）    ← inquiry_embeddings.npy / ChromaDB（BGE 1024 维）
规则风险（队友引擎）        ← 与 FinBERT + LLM 构成三通道抽取
标签对齐（队友）            ← official_company_label_alignment（案例库标签标准化）
```

---

## 六、建议落地顺序

| 优先级 | 动作 | 收益 | 前置 |
|--------|------|------|------|
| P0-1 | 案例检索切 BGE 向量库（读队友 npy/index 或 Chroma） | 检索质量大幅提升 | 队友产物路径可读 |
| P0-2 | 预测模型加 F6（读 365 天监管语义 parquet） | 特征补全，模型提升 | 装 pyarrow；填预测 Agent |
| P1 | 规则风险抽取器 skill（三通道抽取） | 防漏 + 可解释 | 队友共享 risk_dictionary.yaml |
| P2 | F1 时间衰减池化；chunk 参数复用；embedding 缓存共用 | 细节优化 | — |
