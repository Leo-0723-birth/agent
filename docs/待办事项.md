# 待办事项（Backlog）

> 供队员/队长查看与协作。更新：2026-08-24

## 案例匹配 Agent（case-retrieval-agent）

### 【高优】chunk 库覆盖对齐（段落证据覆盖修复）
- **现状**：chunk 段落库仅 **2000 段 / 219 家公司**（构建时用 `--limit` 截断），而案例库为 1483 条精选案例（1478 个公司-日期）、且源数据覆盖 1566 家公司。chunk 证据召回覆盖面远小于案例库。
- **目标**：把 chunk 库覆盖对齐到案例库（或全量）。
- **推荐做法**（已验证，**无需解析 10056 份 PDF**）：
  - 源 JSONL 已在本地：`D:\新建文件夹\02_监管问询\03_语义特征生成Chroma\inquiry_embedding_index.jsonl`（622.7MB，411,898 段，其中 inquiry_letter **38,744 段 / 1566 家公司**）；
  - 重跑全量构建：`python -m backend.scripts.build_chunk_index --fp16`（默认 `--limit 0`=全量；`--fp16` 省一半磁盘）；
  - 成本：CPU 约 1~2.5 小时；磁盘约 80MB（fp16）/160MB（fp32）；会覆盖现有 chunk_db（219 → 1566 家）。
- **备选（仅当要严格对齐 1483 案例库时）**：按 1483 案例库的公司/日期过滤源 JSONL 再构建；否则推荐全量。
- **git 提示**：重建后 `chunk_vectors.npy` 约 80MB，建议评估是否加入 `.gitignore` 或改用 Git LFS。

## 其他待办
- 【后续】案例匹配页已改"案例库规模"为动态读取（依赖 `case_meta.json`，已修 `n_cases=1483`）。
- 【可选】chunk 检索器为独立语义检索（不依赖案例库 ID），确认无需 ID 打通即可独立运行。
