# 待办事项（Backlog）

> 供队员/队长查看与协作。更新：2026-08-24

## 案例匹配 Agent（case-retrieval-agent）

### 【高优】chunk 库覆盖对齐（段落证据覆盖修复）✅ 已完成
- **现状**：chunk 段落库仅 **2000 段 / 219 家公司**（构建时用 `--limit` 截断），而案例库为 1483 条精选案例（1478 个公司-日期）、且源数据覆盖 1566 家公司。chunk 证据召回覆盖面远小于案例库。
- **完成情况（2026-08-24）**：已用云上 GPU（`scripts/cloud_build_chunk_mini.py`）全量重建——**38,744 段 / 1566 家公司**，产物已替换 `backend/data/vector_db/chunk_db.json` + `chunk_vectors.npy`，chunk 检索实测正常。
- **坑**：首次上传源 JSONL 被截断（406M≠622.7M）导致只建出 842 家；压缩成 `.gz`（120MB）后重传成功。
- **git 提示**：`chunk_vectors.npy` 现为 158MB（fp32），建议后续 `.gitignore` 或改 `--fp16`（约 80MB）或 Git LFS。

## 其他待办
- 【后续】案例匹配页已改"案例库规模"为动态读取（依赖 `case_meta.json`，已修 `n_cases=1483`）。
- 【可选】chunk 检索器为独立语义检索（不依赖案例库 ID），确认无需 ID 打通即可独立运行。
