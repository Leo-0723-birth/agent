.venv/
__pycache__/
*.pyc
.env
.vscode/
.idea/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/

# 大体积模型权重（BGE 1.3GB / FinBERT 500MB，不入库；用 HF_HOME 可重下）
backend/models/embedding/
backend/data/cache/
data/cache/

# 大体积向量库（chunk 段落向量 ~160MB(fp32)/80MB(fp16)，可从源 JSONL 重建；不入库）
backend/data/vector_db/chunk_vectors.npy

# 本地备份文件（队员新增）
agent_v1.0_backup.py

# 临时/历史遗留
无关文件夹/
*.log

# Superpowers brainstorming visual companion（仅本地设计讨论，不入库）
.superpowers/

# WorkBuddy 本地会话/工具数据（不入库）
.workbuddy/

# IDE/运行配置（已迁移 FastAPI，streamlit 配置已删除）
.run/
.streamlit/

# 开发日志与本地评测产物
_devlog.txt
_apidevlog.txt
_official_eval/
*.bak

# 运行时输出（报告、trace、搜索缓存），每次运行都会重新生成，不入库
backend/data/output/traces/
backend/data/output/reports/
backend/data/a_stock_list.json
