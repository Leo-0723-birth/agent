# 归因分析 Agent 演练指南（保姆级）

> 演示对象：000004.SZ（国华网安），离线数据，无 LLM 也能完整展示。

---

## 一、准备工作（已全部就绪，不用再做）

| 项 | 状态 |
|----|------|
| 依赖 streamlit / langgraph / PyMuPDF / pdfplumber / dotenv | ✅ 已装 |
| 公告 PDF（62 个） | ✅ 已配好路径（.env） |
| 预测模型 + 建模数据集 | ✅ 齐全 |
| 案例库（4785 条） | ✅ 齐全 |
| 财务离线兜底表 F3-F6 | ✅ 齐全 |

---

## 二、启动（二选一）

### 方式 A：只演示归因页（推荐，端口 8506）

在项目根目录（`...\agent`）打开终端，运行：

```bash
streamlit run 归因分析agent.py --server.port 8506
```

浏览器自动打开（或手动访问）`http://localhost:8506`

### 方式 B：完整导航入口（端口 8501，7 个页面）

```bash
streamlit run 导航入口.py --server.port 8501
```

访问 `http://localhost:8501`，左侧边栏点「归因分析 Agent」。

---

## 三、页面操作步骤

1. **公司代码**：保持默认 `000004.SZ`（或输入"国华网安"）
2. **数据截止日**：默认当天即可（离线数据按 2025-12-02 的口径，建议改成 2025-12-02 更稳定）
3. **最多解析 PDF 数量**：默认 30（够用）
4. **启用扫描 PDF OCR**：保持开（true）
5. **启用 FinBERT**：关（false，避免加载模型）
6. **启用 LLM 归因叙事**：**先关**（false，无 API key 时留空，不影响主展示）
7. 点右下角蓝色 **「开始归因」** 按钮

---

## 四、预期看到什么（按出现顺序）

1. **进度框**：显示"正在运行公告研读→财务检测→预测建模→归因……"
   - ⚠️ 财务检测会先尝试联网（东方财富），**约 30 秒后自动回退离线表**，耐心等，别以为卡死
2. **顶部 4 个指标卡**：诱因数（约 8）、证据引用数、60 天概率、风险等级
3. **Top 风险诱因表格**：特征名、SHAP 贡献、中文说明、标签引用、证据 ID
   - 有证据的显示 `fin_000`；无直接证据的显示"无直接证据"（模型侧信号）
4. **📎 证据池**（展开）：原文证据白名单（财务异常证据 + 公告证据片段）
5. **🔍 审计追踪**（展开）：7 个 Agent 的执行 trace
6. **查看完整归因结果**（展开）：`ctx.attribution` 完整 JSON

---

## 五、如果打开 LLM 叙事（可选，需要 key）

1. 确认项目根 `.env` 里配了 `DEEPSEEK_API_KEY=sk-xxx`
2. 把「启用 LLM 归因叙事」开关打开
3. 归因会多出一段「归因叙事」文字，并自动做**防幻觉校验**（引用的证据编号必须都在证据池里，否则重试/剔除）

---

## 六、常见问题排查

| 现象 | 原因 | 处理 |
|------|------|------|
| 页面打不开 | 端口被占 | 换端口：`--server.port 8507` |
| 卡在"财务检测"很久 | 在线接口超时（正常，约30s） | 等，会自动回退离线表 |
| 证据引用=0 | 没开 LLM 叙事 | 正常；开了 LLM 后会有引用 |
| 案例只有标签通道 | 没装 torch（BGE 语义通道） | 演示时说明"语义通道待 BGE 启用"，标签通道够用 |
| 想单独快速看归因结果 | 不想开网页 | 运行 `python demo_attribution_local.py` |

---

## 七、演示话术（30 秒版）

> "我负责归因分析 Agent——回答『为什么预测这家公司会被问询』。
> 输入 000004.SZ 后，预测模型给出 60 天被问询概率，同时用 SHAP 计算每个特征对概率的贡献；归因 Agent 把黑盒特征名映射成可读的风险因素（比如『现金流异常分位→盈利质量』），再绑定到财务异常/公告的原文证据，并链接 3 个相似历史案例。
> 关键是我加了**防幻觉白名单**：LLM 生成的归因叙事里，每个证据引用都会被校验，引用不在证据池里的编号会被重试或剔除，保证可追溯、不编造。"
