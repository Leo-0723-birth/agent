# D:\competition_agent 项目全面检查报告

> 检查时间：2026-08-29
> 检查范围：代码质量 / 项目结构 / 依赖管理 / 配置文件 / 错误处理 / 性能问题 / 安全性 七大维度
> 检查方式：实际读取源码 + 静态分析，所有结论均有文件行号佐证
> 问题分级：🔴 高（必须修复）/ 🟡 中（建议修复）/ 🟢 低（可优化）

---

## 评分总览

| 维度 | 评分(1-10) | 核心结论 |
|------|-----------|---------|
| 代码质量 | 5.5 | 存在潜伏 NameError、重复方法定义、1354 行巨文件 |
| 项目结构 | 6.0 | 分层清晰但 api 穿透 backend、单文件职责爆炸 |
| 依赖管理 | 5.0 | 无 pyproject.toml、版本策略混乱、含 CVE 旧版本 |
| 配置文件 | 5.5 | .env 含真实密钥、默认值硬编码个人本机路径 |
| 错误处理 | 5.5 | 兜底机制有但过宽、静默吞异常、trace 不记失败 |
| 性能问题 | 6.0 | 超时线程无法终止、串行可并行、同步 IO 阻塞 |
| 安全性 | 4.0 | 鉴权默认关闭、CORS 全开、密钥泄露、无请求限制 |

**综合评分：5.4 / 10** —— 功能可用但工程化不足，存在 1 个不可逆安全风险（密钥泄露）和 1 个潜伏硬故障（llm.py NameError）。

---

## 一、代码质量

### 🔴 1.1 `backend/llm.py` 使用未导入的全局变量，潜伏 NameError

- **文件**：`backend/llm.py:51,53,62,63,73,75,214,215,216`
- **问题**：`llm.py` 在 `_require_key()`、`get_client()`、`_get_lc_base()`、`__main__` 中直接使用 `LLM_API_KEY`、`LLM_BASE_URL`、`LLM_MODEL` 三个变量，但文件中**没有任何 import 语句**引入它们。这三个变量实际定义在 `backend/config.py:70-72`。
- **触发条件**：`RunConfig.use_llm` 默认 `True`（`run_config.py:24`），一旦走实时扫雷且开启 LLM，调用链 `chat_json → chat → get_client() → _require_key()` 立即抛 `NameError: name 'LLM_API_KEY' is not defined`。
- **为何未被发现**：默认离线模式 `use_llm=False` 不触达 LLM，`_apidevlog.txt` 中的成功运行均为离线路径，bug 被掩盖。评审一旦开实时+LLM 即崩。
- **修复**：在 `llm.py` 顶部添加：
  ```python
  from .config import LLM_API_KEY, LLM_BASE_URL, LLM_MODEL
  ```

### 🔴 1.2 `backend/agents/orchestrator.py` 存在重复方法定义（死代码）

- **文件**：`backend/agents/orchestrator.py:80-86` 与 `88-94`
- **问题**：`_agent_callback` 方法被**连续定义了两次**，内容完全相同，第二个覆盖第一个。明显是复制粘贴遗留的笔误。
  ```python
  def _agent_callback(self, agent_key):      # 第 80 行（被覆盖，死代码）
      ...
  def _agent_callback(self, agent_key):      # 第 88 行（实际生效）
      ...
  ```
- **修复**：删除第 80-86 行的重复定义。

### 🟡 1.3 `backend/agents/base.py` 的 `run()` 无异常隔离，单 Agent 崩溃拖垮全链

- **文件**：`backend/agents/base.py:52-77`
- **问题**：`run()` 直接调用 `self.execute(company, ctx)`，无 try/except。子类 `execute()` 抛异常时：① 整条流水线崩溃；② `trace` 不记录失败状态（`trace_log` 缺失该节点）；③ 进度回调停在 5% 不再推进。
- **对比**：`orchestrator.py` 的 `_run_predict/_run_cases/_run_chunks`（159-199 行）有 try/except 兜底，但 `_run_announcement/_run_financial/_run_attribution/_run_report` 没有，依赖 base.run 的健壮性——而 base.run 恰恰没有。
- **修复**：在 `run()` 中包裹 try/except，异常时记录 `trace_complete: False` + error 信息并 re-raise 或返回失败状态。

### 🟡 1.4 `api/pipeline.py` 单文件 1354 行，职责严重混杂

- **文件**：`api/pipeline.py`（实测 1354 行）
- **问题**：单文件混合了 7 类职责：离线报告映射、SHAP/财务/案例字段映射、任务状态机、WebSocket 订阅、LRU 缓存、`StreamingOrchestrator` 类、落盘 trace。`import` 语句还出现在文件中段（654-662 行）而非顶部。
- **修复**：拆分为 `api/mappers.py`（字段映射）+ `api/tasks.py`（任务状态机/缓存）+ `api/streaming.py`（StreamingOrchestrator）+ `pipeline.py`（对外 API）。

### 🟡 1.5 五个 Agent 文件超 500 行，需拆分

- **文件**：`announcement_reader.py`(737行)、`case_retriever.py`(570)、`financial_detector.py`(560)、`predictor.py`(523)、`attributor.py`(509)
- **修复**：按职责拆分，例如 `announcement_reader.py` 把 PDF 下载/OCR/规则抽取/FinBERT/LLM 抽取/F1 聚合下沉到 skills，agent 仅做编排。

### 🟢 1.6 `backend/config.py` 模块导入时执行 `logging.basicConfig`（导入副作用）

- **文件**：`backend/config.py:15-18`
- **问题**：任何 `import backend.config` 都会重配 root logger 格式与级别，影响库使用者和测试。
- **修复**：改为延迟初始化，或仅配置项目自有 logger。

### 🟢 1.7 `sys.path` 手动注入，缺少 `conftest.py`

- **文件**：`api/main.py:19-21`、`api/pipeline.py:15-17`
- **问题**：手动 `sys.path.insert(0, PROJECT_ROOT)`；`backend/tests/` 无同样 hack，从子目录单独跑 pytest 可能 import 失败。项目无 `conftest.py`/`pytest.ini`。
- **修复**：在项目根加 `conftest.py` 或用 `pyproject.toml` 的 `[tool.pytest.ini_options]` 配置 pythonpath。

### 🟢 1.8 三个 Streamlit 测试永久 skip，属死代码

- **文件**：`backend/tests/test_streamlit_app.py`、`test_ui_components.py`、`test_ui_session.py`
- **问题**：顶部 `pytestmark = pytest.mark.skip(reason="...公告研读agent.py 已删除")`，对应入口已在 FastAPI 迁移中删除。
- **修复**：删除这三个 skip 测试。

---

## 二、项目结构

### 🔴 2.1 api 层直接穿透 backend 内部四个子层，无服务边界

- **文件**：`api/main.py:49`、`api/pipeline.py:27,28,683,782,1128`
- **问题**：api 直接 import `backend.skills.stock_code`、`backend.config`、`backend.skills.evidence_policy`、`backend.agents.orchestrator`、`backend.context`——穿透 skills/config/agents/context 四个子层，没有 Service/Facade 边界。
- **正面**：经 grep 验证不存在反向引用（agents → api），无循环依赖。
- **修复**：在 `backend/` 暴露 `services.py`（或 `backend/services/`）作为对 api 的唯一门面。

### 🟡 2.2 根目录冗余 bat 脚本与 devlog 被入库

- **文件**：`start_api.bat`、`启动API服务.bat`、`启动API服务-验证版.bat`、`start_api_verify.bat`、`跑测试.bat`、`_apidevlog.txt`、`_devlog.txt`
- **问题**：`start_api.bat` 与 `启动API服务.bat` 功能重复（均 `uvicorn api.main:app`）；devlog 是调试日志不该入库。`.gitignore:26` 只忽略 `*.log`，未覆盖 `_devlog.txt`。
- **修复**：保留一份启动脚本，其余移入 `scripts/`；devlog 从 git 移除并加入 `.gitignore`。

### 🟡 2.3 `.run/` 与 `.streamlit/` 为失效的孤儿配置

- **文件**：`.run/导航入口.run.xml`、`.streamlit/config.toml`
- **问题**：运行配置指向已删除的 `导航入口.py`（`find` 确认仓库内无此文件）；`.streamlit/config.toml` 对应的 streamlit 入口已随 FastAPI 迁移删除。
- **修复**：删除 `.run/` 与 `.streamlit/` 目录。

### 🟡 2.4 缺少核心流水线端到端集成测试

- **文件**：`api/tests/test_backend_runtime.py`、`test_pipeline.py`
- **问题**：测试用 `monkeypatch.setattr` 把真实流水线 mock 掉，没有任何测试真正跑 `SweepingOrchestrator.execute` 的 7 节点链。
- **修复**：补一条针对固定公司（如 000004.SZ）+ 固定 as_of 的回归集成测试，可关 LLM/FinBERT 但走完规则+财务+预测+案例链。

### 🟢 2.5 README 架构图与代码不一致

- **文件**：`README.md:14,32` vs `api/pipeline.py:710`
- **问题**：README 写"6 Agent 流水线"，但 `StreamingOrchestrator._AGENT_ORDER` 实为 7 个（含 `ChunkRetriever`）。
- **修复**：同步 README 描述为 7 Agent。

### 🟢 2.6 文档散落且与数据混杂

- **文件**：`backend/data/output/`、`backend/data/modeling/` 内混入 7 个 `.md`；根目录 5 个 .md 主题重叠（`框架结构与改进方向.md` vs `优化审查报告.md` vs `完善方案_差距分析与修改建议.md`）。
- **修复**：文档统一归入 `docs/`，数据目录不放 .md。

---

## 三、依赖管理

### 🔴 3.1 无 `pyproject.toml`/`setup.py`，依赖拆两份且无统一安装入口

- **文件**：根 `requirements.txt` 与 `api/requirements.txt` 分离
- **问题**：根 requirements 不含 `fastapi/uvicorn/pydantic`，api requirements 不含 `numpy/torch/langchain`。`start_api.bat:27` 的错误提示只写 `pip install -r requirements.txt`（单数），而根 requirements 根本没有 uvicorn——提示具有误导性，新成员/部署机易漏装。
- **修复**：迁移到 `pyproject.toml`（PEP 621），合并依赖到 `[project.dependencies]`，测试依赖放 `[project.optional-dependencies.test]`。

### 🔴 3.2 `requests>=2.31` 允许安装受 CVE 影响的版本

- **文件**：`requirements.txt:7` `requests>=2.31`
- **CVE**：**CVE-2024-35195**（requests < 2.32.0，urllib3 集成在重定向场景下凭证泄漏/SSRF）
- **修复**：改为 `requests>=2.32.0`（或 `requests>=2.32.4,<3`）。

### 🟡 3.3 版本固定策略前后不一致，破坏可复现性

- **文件**：`requirements.txt`
- **问题**：重型 ML 栈全用宽松 `>=`（`numpy>=1.26`/`pandas>=2.0`/`torch>=2.0`/`transformers>=4.38`/`scipy>=1.11`），而 LangChain 栈用严格 `==`（`langchain-core==1.5.3` 等）。`pip install` 今天和下个月装的 numpy/torch 可能是不同大版本，破坏赛题要求的"可复现"。
- **修复**：要么全锁 `==`（生成 lock 文件），要么至少给 numpy/torch/transformers 加上界。

### 🟡 3.4 `numpy>=1.26` 无上界，可能与 torch/onnxruntime 冲突

- **问题**：numpy 2.0（2024-06 发布）ABI 不兼容 1.x，许多 onnxruntime/rapidocr/torch 旧构建针对 numpy 1.x。`numpy>=1.26` 可解析到 2.x 导致 `ImportError: numpy.core.multiarray failed to import`。
- **修复**：`numpy>=1.26,<2.2`。

### 🟡 3.5 `streamlit>=1.61.1` 版本号异常

- **文件**：`requirements.txt:14`
- **问题**：streamlit 当前稳定版在 1.3x~1.4x 区间，`1.61.1` 既不像有效发布版本也不像占位，疑似笔误（如本意 `1.31` 或 `1.41`）。
- **修复**：核实并改为可解析的真实版本。

### 🟡 3.6 `pytest` 混在生产依赖中，无 dev/prod 分离

- **文件**：`requirements.txt:15` `pytest>=7.0`
- **修复**：移入测试 extras（配合 3.1 的 `pyproject.toml`）。

### 🟢 3.7 `PIL` 未显式声明，靠传递依赖

- **文件**：`backend/tests/test_ocr_extract.py:4` `from PIL import Image`
- **问题**：Pillow 未在任何 requirements 中声明，靠 pdfplumber 传递引入。
- **修复**：显式声明 `pillow>=10`。

### 🟢 3.8 `api/requirements.txt` 把"生产可选"的 websockets/aiohttp 无条件列入

- **文件**：`api/requirements.txt:5-7`
- **问题**：注释"开发/测试 WebSocket 客户端用（生产可选）"但仍会被 `pip install -r` 安装。
- **修复**：移入 `[project.optional-dependencies.dev]`。

---

## 四、配置文件

### 🔴 4.1 `.env` 内含真实 DeepSeek API Key（明文，已泄露）

- **文件**：`.env:11` `DEEPSEEK_API_KEY=sk-***（已轮换，勿复述完整 key）`
- **缓解项**：`.gitignore:4` 已忽略 `.env`，`git check-ignore` 确认忽略，`git ls-files` 无命中——key 未进 git 历史。
- **残留风险**：① 该 key 已暴露于本机磁盘与本次会话；② 比赛交付物目录 `官方要求交付物/` 若按整目录打包提交，`.env` 会随之泄露；③ `.env` 中 ANTHROPIC/OPENAI key 的注释存在 GBK 乱码，文件编码混乱。
- **修复**：**立即在 DeepSeek 控制台轮换该 key**；交付前确保 `.env` 不进入任何 zip；用 UTF-8 重写 `.env.example`。

### 🔴 4.2 `config.py` 默认值硬编码个人本机绝对路径

- **文件**：`backend/config.py:31,32,140,141,142`
- **问题**：
  - L31 `DATA_ROOT = Path(os.getenv("DATA_ROOT", r"D:\BaiduNetdiskDownload"))`
  - L32 `INQUIRY_ROOT = Path(os.getenv("INQUIRY_ROOT", r"D:\新建文件夹"))`
  - L140-142 多个 JSONL/CSV 默认值绑死 `D:\新建文件夹\...`
- **矛盾**：`config.py` 文件头自称"业务代码禁止硬编码路径"，但 config 自身把硬编码路径作为默认值。
- **修复**：默认值改为相对 `BASE_DIR` 的项目内路径，或缺失时抛出明确异常提示设置环境变量。

### 🔴 4.3 `scripts/extract_docx.py` 硬编码个人桌面路径

- **文件**：`scripts/extract_docx.py:5` `path = r"C:/Users/86130/Desktop/06-智能风控...扫雷预警算法探索.docx"`
- **修复**：改为从环境变量或 `config.BASE_DIR` 读取。

### 🟡 4.4 追踪的数据文件内含大量个人本机路径

- **文件**：`backend/data/eval_cache/llm_pdf_predictions.json`（已 git 跟踪）
- **问题**：30+ 条 `"pdf_path": "D:\\BaiduNetdiskDownload\\..."`。另 `backend/data/output/sample_context_000004.json:12`、`backend/skills/announcement_search.py:6,10` 亦有硬编码。
- **修复**：缓存文件改为相对路径或仅存文件名，运行时拼接 `DATA_ROOT`。

### 🟡 4.5 `.gitignore` 遗漏多项应忽略文件

- **问题**：`.gitignore` 未覆盖 `.run/`、`_official_eval/`、`_devlog.txt`、`_apidevlog.txt`、`*.bak`，而 `git ls-files` 显示这些均已被跟踪。`_official_eval/` 还含 `*.bak` 备份文件。
- **修复**：在 `.gitignore` 追加上述条目，并对已跟踪文件执行 `git rm --cached`。

### 🟢 4.6 `.env.example` 注释泄露内部工具名

- **文件**：`.env.example:16` 注释 `本地 ccswitch 代理: http://localhost:xxxx/v1`
- **修复**：从模板清理内部工具名。

---

## 五、错误处理

### 🔴 5.1 `backend/llm.py` 静默吞掉 LangChain 导入异常（无日志）

- **文件**：`backend/llm.py:45` `except Exception: ChatDeepSeek = None`
- **问题**：LangChain 未安装或导入异常时**静默降级**，无任何日志。开发者无法察觉 LangChain 通道实际未生效，排查困难。
- **修复**：改为 `except Exception as e: _logger.warning("LangChain 不可用: %s", e); ChatDeepSeek = None`。

### 🟡 5.2 `base.py` 的 `run()` 无异常捕获，失败节点不记 trace

- **文件**：`backend/agents/base.py:52-77`（详见 1.3）
- **问题**：`execute()` 异常时 trace 不记录失败状态，`trace_log` 缺失该节点，进度回调停在 5%。
- **修复**：包裹 try/except，异常时记录 `trace_complete: False` + error。

### 🟡 5.3 orchestrator 多处 `except Exception` 兜底但不记录堆栈

- **文件**：`backend/agents/orchestrator.py:164,177,196`
- **问题**：`_run_predict/_run_cases/_run_chunks` 用 `except Exception as e` 记录 `reason` 字符串，但不记录完整堆栈（无 `exc_info=True`、无 traceback），复杂错误难以复盘。
- **修复**：`_logger.warning(..., exc_info=True)` 或在 reason 中附 `traceback.format_exc()`。

### 🟡 5.4 `pipeline.py` `_run_agent` 用过宽 `except Exception`

- **文件**：`api/pipeline.py:762` `except Exception as e: # noqa: BLE001`
- **问题**：虽有 `# noqa` 注释承认过宽，但确实吞掉所有异常（包括 `KeyboardInterrupt`、`SystemExit` 的子类以外的一切）。`box["error"]` 仅存异常对象，丢失堆栈。
- **修复**：至少在 error 分支记录 `_logger.warning(..., exc_info=True)`。

### 🟢 5.5 `pipeline.py` `_persist_trace` 同步文件 IO 未保护异常

- **文件**：`api/pipeline.py:1125-1136`
- **问题**：trace 落盘若失败（磁盘满、权限），异常会冒泡到 `run_scan_task` 影响主流程。trace 是审计辅助，不应拖垮主任务。
- **修复**：用 try/except 包裹，失败仅 warning 不影响主流程。

---

## 六、性能问题

### 🔴 6.1 超时后工作线程无法终止，导致资源泄漏

- **文件**：`api/pipeline.py:765-777`、`1259-1273`
- **问题**：实时扫雷用 `asyncio.wait_for(loop.run_in_executor(...), timeout=600)` 设 10 分钟硬超时，但 Python **无法杀死线程**——超时触发 `TimeoutError` 时，底层 `streamer.run()` 的工作线程**继续执行**，持续消耗 CPU/内存/模型资源。`_run_agent` 的 `thread.join(timeout)` 后 `thread.is_alive()` 仍真，代码注释承认了这点。
- **恶化场景**：攻击者通过 `force=true` 反复触发超时任务，每次留下一个无法终止的 daemon 线程，最终耗尽线程池和内存。
- **修复**：对 LLM/HTTP 调用本身设更短的操作级超时；在 Agent 内部更细粒度检查 `cancel_event`；考虑用进程池替代线程池（进程可被终止）。

### 🟡 6.2 6 个 Agent 串行执行，可并行的阶段未并行化

- **文件**：`backend/agents/orchestrator.py:24,110-117`
- **问题**：代码注释明确承认"Phase1-2 与 Phase3-4 各自无依赖，可并行——当前为同步串行"。例如 `_run_financial`（财务检测）和 `_run_predict`（预测建模）无数据依赖却串行。单次实时扫雷可能耗时 5-10 分钟，其中相当一部分本可并行。
- **修复**：将无依赖的 Agent 用 `concurrent.futures.ThreadPoolExecutor` 并行执行，注意模型资源竞争需配合串行锁。

### 🟡 6.3 `_persist_trace` 同步文件 IO 阻塞事件循环

- **文件**：`api/pipeline.py:1125-1136`，调用点 `1279,1283`
- **问题**：`_persist_trace` 用同步 `open(path, "w")` 写 JSONL，直接在 async 函数 `run_scan_task` 中被调用（未包裹 `run_in_executor`），违反 FastAPI 异步最佳实践，会短暂阻塞事件循环，影响 WebSocket 心跳推送。
- **修复**：`await asyncio.to_thread(_persist_trace, ...)`。

### 🟡 6.4 Orchestrator 缓存池无线程安全保障

- **文件**：`api/pipeline.py:670-693`
- **问题**：`_orchestrator_pool` 全局 `OrderedDict`，`get_orchestrator()` 读写时**无锁**。虽有 `_scan_lock` 串行化降低风险，但未来放开并发或离线路径使用该池时存在竞态。
- **修复**：加 `threading.Lock`，或用 `functools.lru_cache` 替代手动 LRU。

### 🟢 6.5 每个 Agent 执行时深拷贝整个 Context

- **文件**：`api/pipeline.py:754-757`
- **问题**：`_run_agent` 对每个 Agent 执行 `copy.deepcopy(value)` 遍历 ctx 所有属性。当 ctx 含上百条公告风险因子时，7 个 Agent 各拷贝一次，累计开销可观。
- **修复**：仅对需隔离的 Agent 深拷贝，或改用 Copy-on-Write。

### 🟢 6.6 向量库整体读入内存，无分页/懒加载

- **文件**：`backend/skills/vector_store.py:37-38`、`chunk_store.py:43-44`
- **问题**：`load()` 用 `json.loads` 和 `np.load` 一次性读入，chunk 向量约 160MB（fp32）。所幸是懒加载且按实例缓存。
- **修复**：对大型向量库考虑 `mmap_mode='r'` 或专用向量数据库。

---

## 七、安全性

### 🔴 7.1 真实 DeepSeek API Key 硬编码在 .env（已泄露）

- **文件**：`.env:11` `DEEPSEEK_API_KEY=sk-***（已轮换，勿复述完整 key）`
- **问题**：这是一个**真实的** DeepSeek API Key（`sk-` 前缀 + 32 位 hex），明文存储。虽 `.gitignore` 已忽略且未入库，但本机磁盘与本次会话已暴露；交付物整目录打包会泄露。
- **攻击场景**：任何获得项目源码（共享、拷贝、误传公开仓库）的人都能提取此 key，调用 DeepSeek API 产生费用或耗尽额度。
- **修复**：**立即轮换该 API Key**；确认 git 历史无此 key（`git log -p -- .env`）；未来 key 只从环境变量注入。

### 🔴 7.2 API 鉴权默认关闭 + 绑定 0.0.0.0

- **文件**：`api/main.py:63-72,295`；`start_api.bat:10`
- **代码**：
  ```python
  API_KEY = os.getenv("API_KEY", "").strip()  # 默认空 = 鉴权关闭
  if API_KEY and request.url.path.startswith("/api/") and ...:  # API_KEY 为空则跳过
  host = os.getenv("API_HOST", "0.0.0.0")  # 默认监听所有网卡
  ```
- **问题**：`API_KEY` 默认空字符串，`if API_KEY and ...` 为 False，**所有 `/api/*` 端点完全无鉴权**。结合 `host=0.0.0.0`，服务暴露整个网络。任何人都能调 `/api/scan`（创建实时任务耗资源）、`/api/analyze`（批量查询）、`/api/reports/{code}/download`（下载报告）。
- **攻击场景**：攻击者扫描内网发现 8000 端口，直接调 `/api/scan?realtime=true` 触发 7-Agent 流水线，占满串行锁，拒绝服务其他用户。
- **修复**：将 `API_KEY` 改为启动时强制要求（无则拒绝启动），或默认绑定 `127.0.0.1`。

### 🔴 7.3 WebSocket 握手鉴权默认关闭且无 Origin 校验

- **文件**：`api/main.py:241-256`
- **代码**：
  ```python
  if API_KEY and websocket.query_params.get("token", "") != API_KEY:
      await websocket.close(code=1008); return
  await websocket.accept()  # API_KEY 为空时直接接受任意连接
  ```
- **问题**：`API_KEY` 默认空时，WebSocket `/ws/pipeline/{task_id}` **无任何鉴权**，也无 Origin 头校验。有效 task_id 的 WS 可被任意客户端连接，监听实时扫雷进度数据（可能含敏感公司风险信息）。
- **攻击场景**：恶意网页通过 JS 建立 `ws://victim-host:8000/ws/pipeline/{已知task_id}` 连接（无 Origin 检查 + CORS 全开），窃取实时扫雷结果。
- **修复**：即使 API_KEY 未配置也应校验 WebSocket 的 Origin 头；对 WS 连接加速率限制。

### 🟡 7.4 CORS 全开（`allow_origins=["*"]`）

- **文件**：`api/main.py:53-59`
- **问题**：虽未设 `allow_credentials=True`（非最危险组合），但 `allow_origins=["*"]` 意味着**任何网站**都能从前端浏览器跨域调用此 API。结合 7.2/7.3 鉴权关闭，任何恶意网页都能代替用户调用 `/api/scan`、`/api/analyze`。
- **修复**：生产环境将 `allow_origins` 限制为具体域名列表，或至少 `http://localhost:*`。

### 🟡 7.5 请求体无大小限制，`AnalyzeRequest.codes` 无长度上限

- **文件**：`api/models.py:94-98`；`api/main.py:84-87`
- **代码**：
  ```python
  class AnalyzeRequest(BaseModel):
      codes: List[str] = Field(..., description="公司代码列表")  # 无 max_length
      window: int = Field(60)  # 无 ge/le 约束（对比 ScanRequest 有 ge=1, le=365）
  ```
- **问题**：`codes` 列表无最大长度限制，`window` 无取值范围校验。`/api/analyze` 是同步路由，对每个 code 调 `offline_to_response` 读盘。攻击者发 10000 个 code 的请求，长时间占用线程池。无任何速率限制。
- **攻击场景**：POST `/api/analyze` body `{"codes": [...重复10000次...]}`，线程池被占满，其他请求排队。
- **修复**：`codes` 加 `Field(..., max_length=50)`；`window` 加 `ge=1, le=365`；引入速率限制中间件（如 `slowapi`）。

### 🟡 7.6 前端搜索下拉框 XSS 防护缺口

- **文件**：`api/static/index.html:2811-2817`
- **代码**：
  ```javascript
  dd.innerHTML = matches.map((c, i) => `
    <div ... data-code="${c.code}" onclick="selectSearchItem('${c.code}')">
      <span class="dd-name">${c.name}</span>   // ← 未调用 escapeHtml()！
  `).join('');
  ```
- **问题**：`c.code` 和 `c.name` 直接拼入 `innerHTML`，**未调用 `escapeHtml()`**。对比同文件其他渲染函数（如 `renderAnnouncementRisks` 第 2443-2445 行）都正确使用了 `escapeHtml()`。虽当前数据源受信任，但这是纵深防御缺口——若报告数据被污染（恶意公司名 `<img src=x onerror=alert(1)>`），将执行任意 JS。
- **修复**：`${c.name}` 改为 `${escapeHtml(c.name)}`，`${c.code}` 同理。

### 🟡 7.7 公告 PDF 下载 URL 来自外部 API 响应（SSRF 风险）

- **文件**：`backend/skills/announcement_search.py:265,287,318`
- **问题**：`pdf_url` 的 path 部分（`adjunct`）来自巨潮 API 响应数据，随后直接用于 HTTP GET。虽 host 硬编码为 `static.cninfo.com.cn`（限制了协议和域名），但若 MITM 巨潮 API，可注入恶意 path。缓解：第 321 行有 50MB 大小限制，第 323 行校验 `%PDF` 魔数。
- **修复**：对 `adjunct` 做白名单校验（仅允许 `公告/` 前缀）；用 `urllib.parse.urlparse` 校验最终 URL 的 host。

### 🟢 7.8 路径穿越防护到位（正面）

- **文件**：`backend/skills/stock_code.py:38-80`、`api/main.py:99-112`
- **评价**：`/api/reports/{code}/download` 调 `normalize_stock_code(code)` 严格校验 6 位数字 + 交易所后缀，拒绝非数字字符和路径分隔符。文件路径从 manifest.json 字段读取，不直接拼接用户输入。**无路径穿越风险**。

### 🟢 7.9 无命令注入风险（正面）

- **评价**：Grep 搜索 `subprocess|os.system|shell=True|eval(|exec(|pickle.load` 仅发现 `model.eval()`（PyTorch 评估模式，非 Python `eval`）和 `ast.literal_eval`（仅解析字面量，安全）。**无命令注入风险**。

### 🟢 7.10 静态文件挂载不暴露 .env（正面）

- **文件**：`api/main.py:287`
- **评价**：`StaticFiles(directory=api/static/)`，而 `.env` 在项目根，不在 static 目录下。**前端无法通过 `/static/.env` 访问密钥**。

---

## 八、优先修复清单（按紧急度排序）

### 必须立即处理（不可逆风险）

1. **🔴 轮换并清理 `.env` 中的真实 DeepSeek API Key**（4.1 / 7.1）
   - 已发生的事实泄露，密钥可能已在共享/拷贝中扩散。先作废旧 key 再生成新 key，新 key 绝不写入任何项目文件，只从环境变量注入。

2. **🔴 修复 `backend/llm.py` 未导入 `LLM_API_KEY` 的 NameError**（1.1）
   - 潜伏在"实时扫雷 use_llm=True"路径下的硬故障，一行 import 即可修。当前被离线模式掩盖，评审一旦开实时+LLM 即崩。

3. **🔴 将 API/WebSocket 鉴权改为默认启用**（7.2 / 7.3）
   - `API_KEY` 启动时强制要求，或默认 host 从 `0.0.0.0` 改 `127.0.0.1`；WebSocket 增加 Origin 头校验。这是阻止未授权访问和 DoS 的第一道防线。

### 交付前应修复

4. **🔴 `requests>=2.31` 升到 `>=2.32.0`**（3.2）——一行改动消除 CVE-2024-35195。
5. **🔴 拆分 `api/pipeline.py`（1354 行）并建立 backend 服务门面**（1.4 / 2.1）——可维护性根因。
6. **🔴 `config.py` 默认值移除个人本机绝对路径**（4.2 / 4.3）——可移植性根因。
7. **🟡 收紧 CORS + 请求体限制**（7.4 / 7.5）——缩小攻击面。
8. **🟡 修复前端搜索框 XSS 缺口**（7.6）——加 `escapeHtml()`。
9. **🟡 删除 `orchestrator.py` 重复方法定义**（1.2）——删 7 行死代码。
10. **🟡 `base.py` 的 `run()` 加异常隔离**（1.3 / 5.2）——单 Agent 崩溃不拖垮全链。

### 工程卫生（建议处理）

11. **🟡 迁移到 `pyproject.toml`，统一依赖管理**（3.1 / 3.3）
12. **🟡 清理入库的 devlog/失效 run 配置/skip 测试/重复 bat**（2.2 / 2.3 / 1.8 / 4.5）
13. **🟡 补充核心流水线端到端集成测试**（2.4）
14. **🟢 Agent 串行改并行、超时线程治理**（6.1 / 6.2）

---

*报告基于实际源码静态审查，未执行动态测试。建议结合 `pytest` 运行与负载测试进一步验证性能与稳定性结论。*
