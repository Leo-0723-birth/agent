# 扫雷预警系统 依赖
python-dotenv>=1.0      # .env 密钥加载
numpy>=1.26
pandas>=2.0
scipy>=1.11             # F2 特征计算（Beneish/Benford 等）
PyYAML>=6.0             # 官方风险标签与词典配置
requests>=2.31          # 东方财富/腾讯接口
PyMuPDF>=1.23           # PDF 解析（公告/问询函）
rapidocr>=3.9,<4        # 扫描型 PDF OCR
onnxruntime>=1.29,<2    # RapidOCR 推理运行时
torch>=2.0              # FinBERT2-base / BGE 向量
transformers>=4.38
huggingface-hub>=0.21  # 固定 revision 下载/校验公告 NLP 模型
streamlit>=1.61.1       # 公告研读独立展示页与主系统演示页
pytest>=7.0             # 单元测试

lightgbm>=4.0            # PredictorAgent（三模型集成之一）
xgboost>=2.0             # PredictorAgent（三模型集成之一，含 XGBoost 推理）
scikit-learn>=1.4        # RF / 概率校准 / 评估指标
shap>=0.46               # RF SHAP 解释
joblib>=1.3              # RF 模型与校准器持久化
pyarrow>=15.0            # 读 modeling_dataset.parquet（可选）
pdfplumber>=0.11         # F6 问询函/年报 PDF 解析（crawl_inquiry 依赖）

# --- Agentic 编排（LangChain + LangGraph，锁 1.x 同一大版本线，升级需单独评估）---
langchain-core==1.5.3     # LLM 应用框架核心（结构化输出/重试/可观测）
langchain-deepseek==1.1.0 # DeepSeek 官方适配器（ChatDeepSeek）
langgraph==1.2.10         # 图编排引擎（StateGraph/节点/条件路由/checkpointer）
langgraph-checkpoint==4.2.0  # 断点续跑/回放（MemorySaver 等）
