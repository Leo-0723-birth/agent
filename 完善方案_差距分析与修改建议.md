# 完善方案：现有交付物 vs 官方规范 — 差距分析与修改建议

> 生成时间：2026-08-27
> 依据输入：桌面 `06-...-...docx`（赛题说明）+ `D:\competition_agent\官方要求交付物\`（当前交付包）+ 仓库 `backend/`、`docs/` 中的实际资产

---

## 0. 分析前提与对象界定（请先确认）

我将你给的两个输入理解为：

| 输入 | 我的判定 | 在方案中的角色 |
|---|---|---|
| 桌面 `.docx` | 东吴证券《赛题说明》（揭榜挂帅）：含命题单位、业务场景、**技术攻关问题、攻关任务、任务目标（技术指标）、数据集描述** | **“标准/标尺”**——官方规范与要求来源 |
| `官方要求交付物/` | 团队当前交付包（README + 06/07/08/09 等文档、03 测试结果、04 API、05 证据案例） | **“现状”**——被审查对象 |

**重要说明**：该 docx 的内容本身即为赛题说明正文，并非一份“团队自拟方案/申报书”。因此它最适合作为**官方标准基线**来对照你的交付包。

⚠️ **若你另有一份“团队自拟的方案/申报书/算法文档”想让我审查（比如一份你写的 Word 提案），请告诉我，我会切换审查对象。** 本方案默认以“交付包是否符合赛题说明”为审查主线。

**额外发现（贯穿全文）**：仓库内（`backend/data/output/任务1交付报告.md`、`backend/scripts/evaluate_case_retriever.py`、`docs/风险词典覆盖报告*.md`、`标注规范_risk_taxonomy_v1.1`）存在比交付包报告**更丰富、更扎实的评测资产**，但当前交付包（尤其 `07_模型评估报告`、`09_交付验收清单`）**未把它们纳入或引用**。这是性价比最高的一类改进。

---

## 1. 官方标准基线（取自赛题说明“任务目标”）

### 1.1 技术指标（8 项）

| 类别 | 指标 | 官方要求 |
|---|---|---|
| 预测 | 60 天 AUC | ≥ 0.75 |
| 预测 | Top 10% 高风险覆盖率 | ≥ 35% |
| 预测 | 问询/未问询二分类 F1 | ≥ 0.65 |
| 语义/归因 | 监管关注点分类准确率 | ≥ 80% |
| 语义/归因 | 关键证据片段召回率 | ≥ 85% |
| 语义/归因 | 相似历史案例 Top-5 命中率 | ≥ 70% |
| 系统/可解释 | Agent 推理链路可追踪率 | 100% |
| 系统/可解释 | 逻辑解释有效性（专家/LLM 打分） | ≥ 85 分 |

### 1.2 成果交付（4 类）

1. 完整源代码（数据处理、特征构建、预测、Agent 编排、报告生成）+ 环境配置与运行脚本
2. 可运行扫雷原型 / API（输入公司代码或列表 → 输出预测）
3. 测试集预测结果数据表、Agent 推理日志、关键证据片段、典型案例分析报告
4. 技术答辩 PPT、算法架构设计说明、模型评估报告、部署说明

### 1.3 攻关任务（内容维度，决定文档应覆盖的主题）

- 任务1 监管问询语料解析与风险关注点抽取（长文本解析 + 关注点标签体系 + 证据片段库）
- 任务2 被问询概率预测模型（多模型集成，30/60/90 天，风险等级排序）
- 任务3 自主规划型扫雷 Agent 与 Skill 编排（多智能体协作 + Skill 封装 + 自动化 Pipeline）
- 任务4 可解释预警报告与复核工具（概率、诱因、案例、原文证据、窗口、置信度；可运行原型/API/Skill）

---

## 2. 差距分析（现状 vs 标准）

### 2.1 模型性能指标

| 指标 | 官方要求 | 当前状态 | 结论 | 说明 |
|---|---:|---:|---|---|
| 60 天 AUC | ≥ 0.75 | **0.8312** | ✅ 达标 | 来源 `model_summary.json` / `03` 数据表，一致 |
| Top 10% 覆盖率 | ≥ 35% | **46.33%** | ✅ 达标 | 一致 |
| 二分类 F1（60天） | ≥ 0.65 | **0.3368** | ❌ **未达标** | 最大实质性缺口；Precision 0.3547 / Recall 0.3207 均低 |
| 监管关注点分类准确率 | ≥ 80% | **92.6%** | ✅ 达标 | **数值达标，但方法学未在交付包中引用**（金标准 300 条、Kappa 0.889 在 `任务1交付报告.md`，未交叉引用） |
| 关键证据片段召回率 | ≥ 85% | 无独立金标准统计 | ⚠️ 待评 | 仓库有 `风险词典覆盖报告` 可作基础，交付包未汇总 |
| 相似案例 Top-5 命中率 | ≥ 70% | 无统一测试表 | ⚠️ 待评 | 仓库有 `evaluate_case_retriever.py` 可产出（内部代理评测），未跑通归档 |
| Agent 可追踪率 | 100% | 设计覆盖 | ✅ 达标（工程能力） | trace + evidence_id 设计覆盖 |
| 逻辑解释有效性 | ≥ 85 分 | 无评分口径/结果 | ⚠️ 待评 | 缺 rubric 与打分结果 |

> 附带：30 天 F1 仅 0.1745、90 天 F1 0.3767，官方仅对 60 天 F1 设硬线（≥0.65），但 30/90 天的虚弱也应说明，避免评委质疑泛化性。

### 2.2 成果交付完整性

| 交付项 | 对应目录 | 状态 |
|---|---|---|
| 完整源代码 + 配置/脚本 | `01_项目源代码与配置/` + `backend/` | ✅ 已具备（7 Agent + LangGraph + 模型/案例资产齐备） |
| 可运行原型 / API | `04_API与运行脚本/` | ✅ 已具备（`/health` 验证通过） |
| 测试集结果表 / 推理日志 / 证据 / 案例 | `02` / `03` / `05` | ✅ 已具备 |
| 算法架构说明 / 模型评估报告 / 部署说明 | `06` / `07` / `08` | ✅ 已具备 |
| **技术答辩 PPT** | `00_技术答辩PPT/` | ❌ **缺失（空目录）** |

### 2.3 文档严谨性与一致性

- ✅ 数字一致性好：`07` 与 `03` 的 AUC / Top10% / F1 与 `model_summary.json` 一致；旧版 0.8335/46.1% 已在 `09` 中明确“不再作为主结果”。
- ⚠️ **评测资产断层（高价值缺口）**：`任务1交付报告.md`（92.6% + 人工金标准 + 8 一级/45 二级体系）、`evaluate_case_retriever.py`、`风险词典覆盖报告`、`标注规范_risk_taxonomy_v1.1` 均在仓库内，却**未纳入/未被交付包报告引用**。结果：`07` 把“证据召回率/案例命中率”写成“待正式评测”，但与资产实际情况脱节。
- ⚠️ **“自主规划型”表述 vs 实际架构**：赛题反复强调“自主规划型”Agent（自主拆解任务、调用 Skill）。当前为**固定 7 节点 LangGraph DAG**（`AnnouncementReader → … → Reporter`），更偏“固定 Pipeline”。它很好地满足了赛题同时要求的“一致、可追踪、可复现”，但弱化了“自主规划”。属于**叙事与诉求的落差**，需在文档中讲清取舍，或补一个可选 Planner/路由层。
- ⚠️ **评测集口径**：当前结果基于团队自有 test set（`model_summary` 生成于 2026-08-25）。赛题明确要求“真实部署前应使用正式封存测试集重新运行 `batch_predict.py` 并归档”。交付包未体现对**官方封存评测集**的跑通与归档。
- ⚠️ **置信度/校准**：任务4 要求输出“模型置信度”。当前有 `data_source` / `coverage` 字段，但**无概率校准曲线/置信度量化展示**，归因与概率的可信度呈现偏弱。
- ⚠️ **脱敏/年份**：赛题要求“隐去真实年份或进行必要脱敏处理”。当前 demo（如 `000004.SZ`、报告期 `20211231`）使用真实代码与日期，需确认是否合规或补充脱敏说明。
- ⚠️ **92.6% 缺方法学锚点**：`07` 直接写“92.6% 达标”，但金标准来源、Kappa、评测集切分均未在交付包内说明，评委难以复核。

### 2.4 “自主规划”与“可复现”的张力（专项）

赛题同时要求「自主规划」与「不同样本一致、可追踪、可复现」。二者存在天然张力：
- 固定 DAG → 强可复现、强可追踪，但弱自主规划；
- 真·规划器 → 强自主规划，但可复现/一致性更难保证。

**建议定位**：以固定主干保证可复现（这符合验收与答辩演示的刚需），再用“可选 Planner/路由”体现自主拆解能力（演示时可展示其决策路径），并在 `06` 架构文档中明确说明这一设计取舍——这正是评委看重的工程成熟度信号。

---

## 3. 具体需要补充 / 调整的部分

按优先级划分（P0 影响达标/验收，P1 高价值且多为“整合已有资产”，P2 完善与一致性）。

### P0 —— 必须，直接影响是否达标 / 能否验收

1. **F1 提升攻坚（建模，最难项）**
   - 根因：类别极不平衡（60 天正样本率 ≈ 5.99%），且当前以“概率排序”为目标，二分类阈值 0.3017 下 Precision/Recall 双低。
   - 方向：类别不平衡处理（class weight / 重采样 SMOTE / focal loss / 代价敏感学习）；**F1 最优阈值搜索**（而非固定 0.5 或验证集阈值）；特征与样本增强；必要时考虑“异常检测式”建模或代价敏感集成。
   - 目标：60 天 F1 从 0.3368 → ≥ 0.65。
2. **官方封存测试集重跑归档**：按赛题要求用正式封存集重跑 `batch_predict.py`，将结果写入 `03`，并同步更新 `07` / `09`，明确标注“封存集结果”。
3. **技术答辩 PPT（补齐 `00`）**：按 `00/README.md` 建议页序（赛题目标 → 数据标签 → F1-F6 → 模型指标 → Agent 架构 → 单公司演示 → 归因证据链 → 系统追踪 → 部署验收）产出；可沿用你偏好的**竖版 + 自定义配色**。

### P1 —— 高价值，主要是“把已有资产纳入交付包”（性价比最高）

4. **评测资产整合**：将 `任务1交付报告.md`、`evaluate_case_retriever.py` 的运行结果、`风险词典覆盖报告` 纳入 `07` / `09` / `05`；补出**证据召回率**与**案例 Top-5 命中率**的量化结果，并明确区分“内部代理评测”与“官方正式评测”两种口径。
5. **`07_模型评估报告` 补强方法学小节**：说明金标准来源（300 条双人标注、Cohen's Kappa 0.889）、评测集切分、阈值选择依据，使 92.6% 从“声称”变为“可复核”。
6. **逻辑解释有效性评测**：定义评分 rubric（专家 / LLM 辅助评审），在样本上打分并力争 ≥ 85 分，将 rubric + 结果归档到 `07` / `05`。

### P2 —— 完善与一致性

7. **“自主规划”叙事 + 可选 Planner 层**（见 2.4）。
8. **概率校准 / 置信度可视化**：增加校准曲线、覆盖率标注，`06`/`07` 展示。
9. **脱敏合规确认与说明**：核实真实代码/日期使用是否符合赛题脱敏要求，必要时补充脱敏说明。
10. **全包数字口径统一核查**：确保无任何残留旧值（0.8335 / 46.1%）。
11. **新增“评测集与金标准说明”文档**（可放 `01` 或独立）：集中说明哪些指标已达标、哪些待官方评测、各自口径，避免评委误解。

---

## 4. 最终修改建议清单（可勾选）

| 编号 | 对象文件 / 模块 | 动作 | 优先级 | 验收标志 |
|---|---|---|---|---|
| A1 | 预测模型（RF/LGB/XGB 集成 + 阈值） | 类别不平衡处理 + F1 最优阈值搜索 + 增强 | P0 | 60 天 F1 ≥ 0.65（封存集） |
| A2 | `03_测试集结果/` | 用官方封存集重跑 `batch_predict.py` 并归档 | P0 | `03` 含封存集结果，`07`/`09` 同步 |
| A3 | `00_技术答辩PPT/` | 产出答辩 PPT（建议页序 + 竖版/自定义配色） | P0 | PPT + PDF 入目录 |
| B1 | `07_模型评估报告` / `09_交付验收清单` | 纳入 `任务1交付报告`、`风险词典覆盖报告` 结论 | P1 | 证据召回率/案例命中率有量化值 |
| B2 | `backend/scripts/evaluate_case_retriever.py` | 运行并归档 Top-5 命中率（标注口径） | P1 | `07` 出现案例命中率数字 |
| B3 | `07_模型评估报告` | 新增方法学小节（金标准/Kappa/切分/阈值） | P1 | 92.6% 可复核 |
| B4 | `07` / `05` | 定义逻辑解释评分 rubric 并打分 | P1 | 解释有效性 ≥ 85 分有结果 |
| C1 | `06_算法架构设计说明` | 补“自主规划 vs 可复现”取舍说明 + 可选 Planner | P2 | 文档覆盖该叙事 |
| C2 | `06` / `07` | 增加概率校准曲线 / 置信度展示 | P2 | 有校准图 |
| C3 | 全包 | 脱敏合规核对与说明 | P2 | 合规说明就绪 |
| C4 | 全包 | 数字口径终检（无旧值残留） | P2 | 全包口径一致 |
| C5 | `01` 或新增 | 评测集与金标准说明文档 | P2 | 指标状态一目了然 |

---

## 5. 建议推进顺序（里程碑）

- **M1 资产盘点与整合（P1，低代码）**：跑 `evaluate_case_retriever.py`、汇总 `任务1交付报告` 与 `风险词典覆盖报告` 进 `07`/`09`/`05`。→ 把“待评”尽量变为“可量化/达标”。
- **M2 F1 建模攻坚（P0，高难度）**：不平衡处理 + 阈值优化，目标 ≥ 0.65。
- **M3 封存集重跑**：用官方封存评测集跑通并归档 `03`，更新 `07`/`09`。
- **M4 PPT 与解释有效性打分**：补齐 `00`，完成逻辑解释评分（≥85）。
- **M5 全包一致性终检**：口径、脱敏、叙事（自主规划）统一。

---

## 6. 待你确认 / 补充

1. 是否另有一份“团队自拟方案/申报书”需要我审查？（当前 docx 是赛题说明本身）
2. F1 提升是否允许使用不平衡学习 / 代价敏感等非常规手段？官方是否提供**封存评测集与金标准文件**及其位置？
3. 答辩 PPT 是否沿用你偏好的**竖版 + 自定义配色**？
4. `backend/data/output/任务1交付报告.md` 等资产，是否授权我将其要点整合进交付包文档（仅引用/汇总，不改写评测结论）？

> 本方案仅提供结构化分析与建议清单，未修改任何交付文件，也未给出代码或修改稿，符合你的要求。确认对象与上述问题后，我可进入下一步（整合资产 / 起草补充章节 / 出 PPT 大纲等）。
